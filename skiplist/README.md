## a simple implement for skiplist

* support initialize with comparable object 
* with remove, insert, search, and clear function
* support const_iterator and iterator, but the iterator only support operator ++ and ->